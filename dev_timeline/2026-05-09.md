# 2026-05-09 Development Timeline

## 00:53 KST — Split Teamwork adapter types into teams_type package

- Actor: Hermes
- Branch: `feat/teamwork-typed-transport-hardening`
- Status: completed
- Completed:
  - Added a RED structure regression test requiring `src/omx_remote/adapter_types/teams_type/` and no remaining flat `teamwork_types.py` module.
  - Split the 322-line `teamwork_types.py` file into concept-owned modules:
    - `team_api_envelope.py`
    - `team_api_raw_payloads.py`
    - `team_api_data_specs.py`
    - `team_api_transport_payloads.py`
    - `team_command_transport_payloads.py`
  - Renamed the manual top-level envelope DTO from `TeamApiEnvelopeSpec` to `TeamApiDecodedEnvelope` so `Spec` remains reserved for real `msgspec.Struct` data specs.
  - Updated production and test imports to direct module imports instead of leaving a compatibility re-export shim.
- Verification:
  - RED: `uv run pytest tests/types/test_teamwork_type_package_structure.py -q` first failed because `teams_type/` did not exist and `TeamApiDecodedEnvelope` was missing.
  - Targeted: `uv run pytest tests/teamwork/test_team_api_snapshot.py tests/teamwork/test_team_snapshot.py tests/types/test_teamwork_type_narrowing.py tests/types/test_transport_types.py tests/types/test_teamwork_type_package_structure.py -q` → `84 passed`
  - Full gate: `git diff --check && uv run ruff check src tests && uv run pyrefly check src && uv run pytest -q` → ruff pass, pyrefly `0 errors`, `691 passed`
  - Stale import search: `adapter_types\.teamwork_types|teamwork_types\.|TeamApiEnvelopeSpec` → only structure-test guard references remained.
  - Live read-only smoke: `agent-remote cockpit snapshot`, `runtime status`, `adapt status --target hermes`, and Team API `list-tasks/read-events/mailbox-list/read-monitor-snapshot` all exited 0.
- Next:
  - Open/update the PR for `feat/teamwork-typed-transport-hardening` and review the type-hardening + type-package split together.
- Blockers / non-goals:
  - No mutating OMX operation was executed.
  - No `__init__.py` compatibility bucket or `teamwork_types.py` re-export shim was kept.

## 01:28 KST — Address PR review: optional adapter capability ownership

- Actor: Hermes
- Branch / PR: `feat/teamwork-typed-transport-hardening` / PR #4
- Status: completed
- Review finding:
  - Codex flagged that `load_capabilities_payload` accidentally required `ownership`, while `AdapterCapabilitySnapshot` treats `ownership` as optional.
  - This could reject valid `adapt probe/status/envelope` capability payloads that only include `id`, `label`, `status`, and `summary`.
- Completed:
  - Added a RED regression test for `load_capabilities_payload` accepting schema-supported capability items without `ownership`.
  - Changed `AdapterCapabilityTransportPayload.ownership` to `NotRequired[str]`.
  - Updated `load_capabilities_payload` to preserve `ownership` only when provided and still reject non-string `ownership` values.
  - Repaired the local `.venv` package metadata by removing the stale `agent_remote-0.1.0.dist-info` directory and running `uv sync --reinstall-package agent-remote`; `uv` had been failing with `Resource deadlock avoided` while reading `direct_url.json`.
- Verification:
  - RED: `uv run pytest tests/bridge/test_adapter_probe.py::test_load_capabilities_payload_accepts_schema_supported_missing_ownership -q` first failed with `BridgeSurfaceError: omx adapt probe returned a non-string ownership payload`.
  - Targeted: `uv run pytest tests/bridge/test_adapter_probe.py::test_load_capabilities_payload_accepts_schema_supported_missing_ownership tests/bridge/test_adapter_probe.py::test_probe_adapter_returns_normalized_subset -q` → `2 passed`
  - Bridge suite: `uv run pytest tests/bridge -q` → `39 passed`
  - Full gate: `git diff --check && uv run ruff check src tests && uv run pyrefly check src && uv run pytest -q` → ruff pass, pyrefly `0 errors`, `692 passed`
  - Live read-only smoke: `agent-remote adapt probe/status/envelope --target hermes` all exited 0.
- Blockers / non-goals:
  - No mutating OMX operation was executed.

## 03:01 KST — Cockpit all-status snapshot source inspection

- Actor: Hermes with Codex subagent support
- Branch: `feat/cockpit-all-status-snapshot`
- Status: completed
- Plan:
  - Created local ignored plan `docs/jobs/cockpit-all-status-snapshot/0_overview.md` for a read-only cockpit all-status / Team discovery-inspection slice.
  - Kept scope read-only: no launch, no cleanup, no mutation, no implicit advance.
- Codex subagents:
  - Created isolated worktrees for schema contract, Team discovery, CLI wiring, and review/audit streams under `/private/tmp/omx-codex-cockpit-*`.
  - Used the Team discovery worker output as a rough input, then integrated manually in owner repo to match strict repo contracts.
  - Accepted the review/audit worker finding that current Goal mirror/lifecycle artifacts do not persist actual Team names, so cockpit must not invent Team names from `team_worker_count`, worker IDs, goal IDs, or plan artifacts.
  - Rejected the later wiring worker direction that scanned broad `.omx`/JSON paths and could infer uncontracted Team names; kept the narrower honest first slice.
- Completed:
  - Added `CockpitStatusSourceState` and `CockpitStatusSourceObservation` plus top-level `discovered_teams`, `status_sources`, and `warnings` fields on `CockpitSnapshot`.
  - Added `runtime/cockpit/linked_team_discovery.py` to inspect `.agent-remote/state/codex-goal.json` read-only, report malformed state as warnings, and avoid inventing Team names when only fanout intent exists.
  - Wired `read_cockpit_snapshot(...)` to merge explicit Team names with discovered Team names, read Team evidence only for selected names, and expose status-source/warning evidence in the snapshot.
  - Extended cockpit tests for strict/frozen all-status fields, missing/malformed Goal mirror handling, no invention from `team_worker_count`, explicit/discovered Team merge order, and discovered-Team evidence reads.
  - Rebuilt the local `.venv` (`rm -rf .venv && uv sync`) after the installed package state was corrupted enough for `uv run agent-remote` to miss `omx_remote.cli`.
- Verification:
  - Targeted: `uv run pytest tests/runtime/test_cockpit_snapshot.py -q` → `13 passed`
  - Targeted CLI/runtime: `uv run pytest tests/runtime/test_cockpit_snapshot.py tests/test_cli_entrypoint.py -q` → `50 passed`
  - Full gate: `git diff --check && uv run ruff check src tests && uv run pyrefly check src && uv run pytest -q` → ruff pass, pyrefly `0 errors`, `700 passed`
  - Installed CLI smoke after venv rebuild:
    - `uv run agent-remote --help` → exit 0
    - `uv run agent-remote cockpit snapshot --cwd .` → exit 0; emitted `discovered_teams`, `status_sources`, and `warnings`
    - `uv run agent-remote cockpit snapshot --cwd . --team-name missing-team` → exit 0; `team_selection:observed` and no invented discovered teams
- PR:
  - Commit: `6ff8862 feat: expose cockpit all-status sources`
  - PR: https://github.com/limhaneul12/omx-agent-adapter/pull/5
- Next:
  - Future slice: add a durable Team-name source only when Goal/Ralph/Team lifecycle artifacts actually persist launched Team identity; until then, keep all discovery claims as inspected-source warnings.
- Blockers / non-goals:
  - No mutating OMX operation was executed.
  - No broad `.omx` JSON scanner or plan-artifact-based Team-name inference was included.

## 11:44 KST — Cockpit Team evidence action hardening

- Actor: Hermes
- Branch: `feat/cockpit-team-evidence-actions`
- Status: completed
- Completed:
  - Added RED cockpit tests for active Team evidence blocking mutation and becoming the top-level recommended next action.
  - Added a dedicated `team_evidence` status source so all-status snapshots distinguish skipped Team reads from observed or warning-bearing Team evidence.
  - Updated cockpit top-level action selection so active Team evidence recommends `inspect_team_evidence` instead of falling through to `observe` when runtime mode readers are quiet.
  - Updated cockpit mutation safety so active Team evidence makes `safe_to_mutate=false` even when `active_runtime_modes` is empty.
- Verification:
  - RED: `uv run pytest tests/runtime/test_cockpit_snapshot.py::test_cockpit_treats_active_team_evidence_as_mutation_blocker_and_top_action -q` first failed with `safe_to_mutate=True` and `recommended_next_action='observe'`.
  - RED: `uv run pytest tests/runtime/test_cockpit_snapshot.py::test_read_cockpit_snapshot_exposes_team_evidence_status_source -q` first failed with missing `team_evidence` source.
  - Targeted: `uv run pytest tests/runtime/test_cockpit_snapshot.py -q` → `16 passed`.
  - Full gate: `git diff --check && uv run ruff check src tests && uv run pyrefly check src && uv run pytest -q` → ruff pass, pyrefly `0 errors`, `703 passed`.
  - Installed CLI smoke: `uv sync --no-editable --reinstall-package agent-remote && .venv/bin/agent-remote cockpit snapshot --cwd .` → exit 0; `status_sources_count=7` including `team_evidence`.
- Next:
  - Open PR for review, then continue toward richer cockpit/operating-decision stitching only from evidence-backed lanes.
- Blockers / non-goals:
  - No mutating OMX operation was executed.
  - No Team-name invention or broad `.omx` scanner was introduced.

## 12:10 KST — Address PR #6 review: unknown Team status is degraded, not active

- Actor: Hermes
- Branch / PR: `feat/cockpit-team-evidence-actions` / PR #6
- Status: completed
- Review finding:
  - Codex flagged that the new Team evidence predicate treated every status except `missing`, `inactive`, and `ended` as active.
  - This was objectively valid because `_read_team_observation(...)` uses `status="unknown"` when Team status reads fail, so transient read degradation could incorrectly block mutation and recommend `inspect_team_evidence` as if Team execution were active.
- Completed:
  - Added a RED regression test for unknown Team status with warning-bearing evidence.
  - Changed active Team detection to require explicit active status instead of defaulting unknown/degraded statuses to active.
  - Reused the same predicate for the Ralph -> Team lane state so degraded Team reads remain `unknown`, not `active`.
- Verification:
  - RED: `uv run pytest tests/runtime/test_cockpit_snapshot.py::test_cockpit_treats_unknown_team_status_as_degraded_not_active -q` first failed because the Ralph -> Team lane was `active`.
  - Targeted: `uv run pytest tests/runtime/test_cockpit_snapshot.py -q` → `17 passed`.
  - Full gate: `git diff --check && uv run ruff check src tests && uv run pyrefly check src && uv run pytest -q` → ruff pass, pyrefly `0 errors`, `704 passed`.
  - Installed CLI smoke: `uv sync --no-editable --reinstall-package agent-remote && .venv/bin/agent-remote cockpit snapshot --cwd .` → exit 0; `status_sources_count=7` including `team_evidence`.
- Next:
  - Push review fix, reply to Codex review thread with commit and verification evidence, then merge if PR remains clean.

## 12:25 KST — Cockpit status documentation realignment

- Actor: Hermes
- Branch: `docs/cockpit-status-realignment`
- Status: completed
- Completed:
  - Started a docs-only realignment slice after PR #5 and PR #6 were merged to `main`.
  - Updated the tracked current feature scope so cockpit all-status sources, `team_evidence`, active-Team mutation blocking, and unknown-status degradation are treated as the current baseline instead of pending work.
  - Updated the tracked operating-lanes status next-work list so the next cockpit slice is evidence-reason stitching rather than broad automation.
  - Updated local ignored `docs/jobs/` ledgers to mark cockpit all-status / Team evidence as an implemented baseline and to keep Team identity discovery evidence-backed only.
- Verification:
  - `git diff --check` → pass.
  - `uv run ruff check src tests` → pass.
  - `uv run pyrefly check src` → `0 errors`.
  - `uv run pytest -q` → `704 passed`.
- Next:
  - Commit, push, and open a PR for tracked docs.
- Blockers / non-goals:
  - No production code was changed.
  - No cockpit launch, cleanup, mutation, or implicit Goal advance was added.

## 13:03 KST — Cockpit decision reason stitching

- Actor: Hermes
- Branch: `feat/cockpit-decision-reasons`
- Status: completed
- Completed:
  - Merged PR #7 first, fast-forwarded local `main`, and deleted the local/remote docs branch before starting the next code slice.
  - Added `CockpitDecisionReason` and top-level `decision_reasons` on `CockpitSnapshot` so `safe_to_mutate` / `recommended_next_action` have evidence-backed explanations.
  - Wired cockpit decision reasons for runtime contradictions, active runtime evidence, active Team evidence, Goal awaiting Ralph, and the quiet observe baseline.
  - Kept the slice read-only: no launch, cleanup, mutation, implicit Goal advance, or Team-name invention.
- Verification:
  - RED: `uv run pytest tests/runtime/test_cockpit_snapshot.py::test_cockpit_treats_active_team_evidence_as_mutation_blocker_and_top_action -q` first failed because `CockpitSnapshot` had no `decision_reasons` field.
  - Targeted: `uv run pytest tests/runtime/test_cockpit_snapshot.py -q` → `17 passed`.
  - Static gates: `git diff --check && uv run ruff check src tests && uv run pyrefly check src` → pass, pyrefly `0 errors`.
  - Full gate: `uv run pytest -q` → `704 passed`.
  - Installed CLI smoke: `uv sync --no-editable --reinstall-package agent-remote && .venv/bin/agent-remote cockpit snapshot --cwd .` → exit 0; emitted `decision_reasons_count=1` with `active_runtime_evidence` in the current workspace.
- Next:
  - Commit, push, open PR, request Codex review, then merge after clean review/checks.
- Blockers / non-goals:
  - No active blocker.

## 13:18 KST — Address PR #8 review: align decision reason priority

- Actor: Hermes
- Branch / PR: `feat/cockpit-decision-reasons` / PR #8
- Status: completed
- Review finding:
  - Codex flagged that `decision_reasons` could put `active_runtime_evidence` first when `runtime_status.has_active_modes=True`, while `recommended_next_action` still ignored that fallback and could choose `inspect_team_evidence` or `prepare_ralph`.
  - This was valid because the first decision reason should explain the top-level action priority.
- Completed:
  - Added a RED regression test covering runtime-status-only active evidence plus active Team evidence.
  - Updated `_derive_recommended_next_action(...)` to consider `runtime_status.has_active_modes=True` as `observe_active_runtime`, matching the decision-reason priority and mutation blocker.
- Verification:
  - RED: `uv run pytest tests/runtime/test_cockpit_snapshot.py::test_cockpit_prioritizes_status_runtime_evidence_over_active_team_reason -q` first failed with `recommended_next_action='inspect_team_evidence'`.
  - Targeted: `uv run pytest tests/runtime/test_cockpit_snapshot.py -q` → `18 passed`.
  - Static gates: `git diff --check && uv run ruff check src tests && uv run pyrefly check src` → pass, pyrefly `0 errors`.
  - Full gate: `uv run pytest -q` → `705 passed`.
  - Installed CLI smoke: `uv sync --no-editable --reinstall-package agent-remote && .venv/bin/agent-remote cockpit snapshot --cwd .` → exit 0; emitted `recommended_next_action='observe_active_runtime'` and `active_runtime_evidence` in the current workspace.
- Next:
  - Push review fix, reply to Codex review thread with commit and verification evidence, then re-check PR #8.

## 14:04 KST — Cockpit durable Team discovery and inactive runtime correction

- Actor: Hermes
- Branch: `fix/cockpit-runtime-active-inference`
- Status: completed locally; PR pending
- Completed:
  - Checked cockpit/runtime evidence before coding and found current OMX status output only contained `inactive (phase: ...)` rows after stale local runtime state cleanup, but cockpit still treated `runtime_status.has_active_modes=True` as active evidence.
  - Added a RED runtime regression test for phase-suffixed inactive-only `omx status` output, then changed active-mode inference to use parsed per-mode statuses when available instead of treating any non-`No active modes.` stdout as active.
  - Added RED cockpit Team-discovery coverage for exact persisted Team names in the Goal mirror state.
  - Added `linked_team_names` to `CodexGoalMirrorState` and wired `discover_linked_team_names(...)` to expose those exact persisted names while preserving the existing no-invention warning when only `team_worker_count` is present.
- Verification:
  - RED: `uv run pytest tests/runtime/test_runtime_snapshot.py::test_read_runtime_status_treats_inactive_only_phase_lines_as_idle -q` first failed with `has_active_modes=True`.
  - RED: `uv run pytest tests/runtime/test_cockpit_snapshot.py::test_discovery_discovers_exact_team_names_from_goal_mirror_state -q` first failed with `discovered_team_names=()`.
  - Targeted: `uv run pytest tests/runtime/test_runtime_snapshot.py tests/runtime/test_cockpit_snapshot.py tests/runtime/test_codex_goal_runtime.py tests/runtime/test_goal_lifecycle_artifacts.py -q` → `52 passed`.
  - Full gate: `git diff --check && uv run ruff check src tests && uv run pyrefly check src && uv run pytest -q` → ruff pass, pyrefly `0 errors`, `707 passed`.
  - Installed CLI smoke: `uv sync --no-editable --reinstall-package agent-remote && .venv/bin/agent-remote cockpit snapshot --cwd .` → exit 0; emitted `safe_to_mutate=True`, `recommended_next_action='observe'`, and no active runtime evidence in the current inactive workspace.
- Next:
  - Commit, push, open PR, request Codex review, then objectively inspect review findings before merge.
- Blockers / non-goals:
  - No broad `.omx` scanner, Team-name invention, launch, cleanup command surface, or implicit Goal advance was added.

## 14:34 KST — Address PR #9 review: preserve unknown runtime uncertainty

- Actor: Hermes
- Branch / PR: `fix/cockpit-runtime-active-inference` / PR #9
- Status: completed locally; PR update pending
- Review finding:
  - Codex flagged that parsed `unknown` runtime mode statuses were treated the same as inactive modes because `_infer_has_active_modes(...)` returned `False` whenever no parsed mode was explicitly active.
  - This was objectively valid: a live but unrecognized runtime status token could make cockpit mark mutation as safe.
- Completed:
  - Added a RED runtime regression test for mixed `unknown`/idle parsed mode statuses preserving `has_active_modes=None`.
  - Added cockpit regression coverage so uncertain runtime activity blocks mutation, recommends `inspect_runtime_status`, and explains the action through `runtime_status_uncertain`.
  - Updated runtime inference to return `True` for any active parsed mode, `None` for any unknown parsed mode without active evidence, and `False` only when all parsed statuses are known inactive/idle-like values.
  - Updated cockpit source/action/reasoning so uncertain runtime status is surfaced as unknown runtime evidence and is not treated as safe-to-mutate.
- Verification:
  - RED: `uv run pytest tests/runtime/test_runtime_snapshot.py::test_read_runtime_status_preserves_uncertainty_for_unknown_only_modes tests/runtime/test_cockpit_snapshot.py::test_cockpit_treats_uncertain_runtime_status_as_mutation_blocker -q` first failed with `has_active_modes=False` and `safe_to_mutate=True`.
  - Targeted: `uv run pytest tests/runtime/test_runtime_snapshot.py tests/runtime/test_cockpit_snapshot.py tests/runtime/test_codex_goal_runtime.py tests/runtime/test_goal_lifecycle_artifacts.py -q` → `54 passed`.
  - Full gate: `git diff --check && uv run ruff check src tests && uv run pyrefly check src && uv run pytest -q` → ruff pass, pyrefly `0 errors`, `709 passed`.
  - Installed CLI smoke: `uv sync --no-editable --reinstall-package agent-remote && .venv/bin/agent-remote cockpit snapshot --cwd .` → exit 0; current workspace still emits `safe_to_mutate=True`, `recommended_next_action='observe'`, and `no_blocking_evidence` because no unknown runtime status is present.
- Next:
  - Commit and push the PR #9 review fix, reply to the Codex review thread with verification evidence, then request re-review.

## 15:24 KST — Start PR status source cockpit slice

- Actor: Hermes
- Branch: `feat/cockpit-pr-status-source`
- Status: in_progress
- Starting point:
  - PR #9 is merged to `main` as `4d33e0317af842fe124b1f84916e198eeec25370`.
  - Local `main` is clean and aligned with `origin/main`.
  - Current cockpit smoke reports `safe_to_mutate=true`, `recommended_next_action=observe`, no warnings, and no discovered Teams.
- Intended scope:
  - Add a read-only cockpit status source for GitHub PR / review / check evidence for the current branch.
  - Keep credential handling non-printing and optional: missing credentials or non-PR branches should degrade to skipped/missing status, not break cockpit.
  - Preserve cockpit as observability only: no merge, launch, cleanup, or implicit Goal advance.
- TDD plan:
  - Add tests for PR source injection/summary behavior first and confirm RED.
  - Implement the smallest schema/runtime wiring needed to expose the source.
  - Then add optional live GitHub reader only after the pure cockpit contract is locked.
- Verification plan:
  - Targeted cockpit/runtime tests.
  - `git diff --check`, `uv run ruff check src tests`, `uv run pyrefly check src`, `uv run pytest -q`.
  - Non-editable installed CLI smoke with `.venv/bin/agent-remote cockpit snapshot --cwd .`.
- Non-goals:
  - No mutation/merge commands from cockpit.
  - No token printing or credential persistence.
  - No CI-gate enforcement beyond read-only evidence surfacing.

## 15:37 KST — PR status source implementation checkpoint

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Changes:
  - Added `CockpitPullRequestObservation` and optional `CockpitSnapshot.pull_request_status`.
  - Wired cockpit to include a `github_pr_status` read-only status source.
  - Added a GitHub PR reader that uses the current branch plus GitHub REST API evidence for open PR, Codex review/no-major-issues comments, combined commit status, and check-runs.
  - Credential handling remains non-printing and optional; missing/API-failed PR query yields `status=unavailable` with a warning rather than falsely reporting no PR.
- RED/GREEN evidence so far:
  - New GitHub status tests first failed on missing `_run_git_command` / `_read_github_api_json` helpers.
  - Added API-unavailable regression first failed because `None` API payload was incorrectly classified as `no_open_pull_request`; fixed to preserve unavailable state.
  - Targeted cockpit/GitHub tests now pass.
- Verification so far:
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `uv run pytest tests/runtime/test_cockpit_snapshot.py tests/runtime/test_github_pr_status.py -q` pass (`24 passed`).
- Next:
  - Run full gate plus non-editable installed cockpit smoke, then commit/push/open PR.

## 16:18 KST — Address PR #10 review: base-agnostic PR lookup and review precedence

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P1: `base=main` in the current-branch open PR lookup can falsely report `no_open_pull_request` for non-main/backport/release-base PRs.
  - Codex P2: Codex no-major-issues comments were evaluated before blocking GitHub review states, so `CHANGES_REQUESTED` could be masked.
- Objective assessment:
  - Both comments are valid cockpit evidence correctness issues and should block merge until fixed.
- TDD evidence:
  - Updated PR lookup tests to expect `GET /pulls?head=<owner>:<branch>&state=open` without a hardcoded base filter.
  - Added regression coverage for `CHANGES_REQUESTED` reviews taking precedence over Codex no-major-issues issue comments.
  - RED observed: focused GitHub status tests failed because production still emitted `&base=main` in the query path.
- Fix:
  - Removed the hardcoded `base=main` query parameter.
  - Reordered review classification so blocking review states are inspected before Codex no-major-issues issue comments.
- Verification so far:
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`4 passed`).
  - `uv run pytest tests/runtime/test_cockpit_snapshot.py tests/runtime/test_github_pr_status.py -q` pass (`25 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
- Next:
  - Run full gate, installed CLI smoke, then push fix commit and reply to Codex review comments.

## 16:22 KST — PR #10 review fix full verification

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Verification:
  - `git diff --check` pass.
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `uv run pytest -q` pass (`714 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` exposes `snapshot`.
  - `.venv/bin/agent-remote cockpit snapshot --help` exposes `--cwd`.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe` before this fix commit was pushed.
- Next:
  - Commit/push the review fix, reply to Codex review comments with evidence, and request a fresh Codex review.

## 17:06 KST — Address PR #10 follow-up review: stale reviewer decisions

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P2: `_classify_review_state(...)` treated any historical `CHANGES_REQUESTED` review as current blocking evidence even if the same reviewer later approved.
- Objective assessment:
  - Valid cockpit evidence freshness issue. GitHub review payloads are chronological; current review state should be derived from each reviewer's latest decisive review, not the first historical blocker.
- TDD evidence:
  - Added regression coverage for one reviewer first requesting changes and then approving; expected `review_state=approved` when no Codex no-major-issues marker is present.
  - RED observed: focused GitHub status tests failed with `changes_requested` before the fix.
- Fix:
  - Added reviewer identity key extraction from review payload `user.login`, with per-review fallback for anonymous payloads.
  - `_classify_review_state(...)` now tracks latest decisive review state per reviewer (`approved` / `changes_requested`) before classifying blockers.
  - Blocking latest reviewer decisions still outrank Codex no-major-issues comments; stale superseded blockers no longer do.
- Verification:
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`5 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`715 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` exposes `snapshot`.
  - `.venv/bin/agent-remote cockpit snapshot --help` exposes `--cwd`.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe` before this follow-up commit was pushed.
- Next:
  - Commit/push the follow-up fix, reply to Codex follow-up comment, and request another Codex review.

## 17:28 KST — Address PR #10 follow-up review: paginated review decisions

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P2: latest-reviewer-decision classification requested only `/pulls/{number}/reviews` once, so GitHub's default first page could omit newer review decisions on PRs with more than 30 reviews.
- Objective assessment:
  - Valid evidence freshness issue. The prior fix depends on chronological latest decisive reviews, so the source must fetch later review pages before classifying.
- TDD evidence:
  - Added regression coverage where page 1 contains 100 old `CHANGES_REQUESTED` reviews and page 2 contains the same reviewer's later `APPROVED` decision.
  - RED observed: focused GitHub status tests failed because production still requested the unpaginated reviews endpoint.
- Fix:
  - Added explicit GitHub array pagination with `per_page=100` and one-indexed `page` parameters.
  - `read_github_pull_request_status` now reads all review pages before deriving reviewer-latest decisions.
- Verification so far:
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`6 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`716 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` exposes `snapshot`.
  - `.venv/bin/agent-remote cockpit snapshot --help` exposes `--cwd`.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe` before this follow-up commit was pushed.
- Next:
  - Commit/push and close the Codex review thread with verification evidence.

## 17:40 KST — Address PR #10 follow-up review: issue comments pagination and non-interactive credentials

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P1: `/issues/{number}/comments` is paginated and ordered oldest-first, so a later Codex no-major-issues marker can be missed after 30+ comments.
  - Codex P2: `git credential fill` can prompt or hang when no cached credential/env token exists unless credential lookup is explicitly non-interactive and bounded.
- Objective assessment:
  - Both comments are valid cockpit reliability/evidence issues. Codex marker classification should read all issue-comment pages, and cockpit snapshot should degrade quickly when credentials are missing rather than blocking for prompts.
- TDD evidence:
  - Added regression coverage where issue-comment page 1 contains 100 older comments and page 2 contains `Didn't find any major issues. Breezy!`; expected `review_state=codex_no_major_issues`.
  - Added regression coverage that git credential lookup sets non-interactive git environment and a timeout.
  - RED observed: focused tests failed with `pending_or_unreviewed` for the later issue-comment page and missing `timeout` kwargs for `git credential fill`.
- Fix:
  - Reused the GitHub paginated array reader for issue comments as well as reviews.
  - Added non-interactive credential env (`GIT_TERMINAL_PROMPT=0`, `GCM_INTERACTIVE=never`) and a bounded `git credential fill` timeout, with timeout treated as missing credential.
- Verification so far:
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`8 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`718 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe`.
- Next:
  - Commit/push, reply to Codex comments, and request another review.


## 17:50 KST — Address PR #10 follow-up review: unavailable review evidence stays unknown

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P1: if PR review pages cannot be fetched but issue comments can, review classification could still return `codex_no_major_issues` from a marker comment despite missing possible blocking review evidence.
- Objective assessment:
  - Valid evidence completeness issue. Codex no-major-issues markers are only safe to use after the review endpoint is available, because blocking latest reviewer decisions must remain higher priority.
- TDD evidence:
  - Added regression coverage where the reviews page returns unavailable (`None`) while issue comments contain `Didn't find any major issues. Breezy!`; expected `review_state=unknown`.
  - RED observed: focused test failed with `codex_no_major_issues` before the fix.
- Fix:
  - `_classify_review_state(...)` now returns `unknown` immediately when the reviews payload is unavailable/malformed, before inspecting Codex issue comments. Empty review arrays still classify through the normal pending/Codex-marker path.
- Verification so far:
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`9 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`719 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe`.
- Next:
  - Commit/push, reply to Codex, and request another review.


## 17:58 KST — Address PR #10 follow-up review: paginated check-run evidence

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P2: `/commits/{sha}/check-runs` is paginated, so reading only the first page can miss a failing check run on page 2+ and misclassify `check_state`.
- Objective assessment:
  - Valid evidence completeness issue for larger CI matrices. Cockpit check-state classification must inspect all check-run pages before deciding success/pending/failure.
- TDD evidence:
  - Added regression coverage where check-run page 1 contains 100 successful completed runs and page 2 contains a completed failure; expected `check_state=failure`.
  - RED observed: focused test failed with `success` before the fix because production still read the unpaginated check-runs endpoint.
- Fix:
  - Added a GitHub object-wrapped array pagination helper for endpoints like check-runs.
  - `read_github_pull_request_status` now reads `/commits/{sha}/check-runs?per_page=100&page=N` until the final short page before deriving `check_state`.
- Verification so far:
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`10 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`720 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe`.
- Next:
  - Commit/push, reply to Codex, and request another review.


## 18:12 KST — Address PR #10 follow-up review: dismissed reviews and Codex marker authorship

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P1: a later `DISMISSED` review should clear the same reviewer's prior `CHANGES_REQUESTED`; otherwise cockpit can report a stale blocker.
  - Codex P2: issue-comment `Didn't find any major issues` markers must be trusted only when authored by the Codex bot; arbitrary human/bot comments should not force `codex_no_major_issues`.
- Objective assessment:
  - Both findings are valid evidence-correctness issues. GitHub can dismiss stale blocking reviews, and free-text marker matching without author validation is spoofable/misleading.
- TDD evidence:
  - Added RED coverage for `CHANGES_REQUESTED` followed by same-reviewer `DISMISSED`, expecting `review_state=pending_or_unreviewed`; pre-fix result was `changes_requested`.
  - Added RED coverage for a non-Codex author containing the clean-review marker text, expecting `review_state=pending_or_unreviewed`; pre-fix result was `codex_no_major_issues`.
- Fix:
  - Review classification now treats `dismissed` as clearing the reviewer key from latest decisive decisions.
  - Codex clean-review issue comments now require `user.login == chatgpt-codex-connector[bot]` before accepting the marker.
- Verification so far:
  - Targeted RED tests confirmed failure before implementation.
  - Targeted GREEN tests pass.
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`12 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`722 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe`.
- Next:
  - Commit/push, reply to Codex, and request another review.


## 18:27 KST — Address PR #10 follow-up review: unknown checks and path-scoped credentials

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P1: `_classify_check_state` could return `success` when commit status is success but check-runs evidence is `unknown` because the check-runs API read failed.
  - Codex P2: `git credential fill` used only `protocol=https` and `host=github.com`, omitting repository `path`, which can fail or select the wrong credential when `credential.useHttpPath=true`.
- Objective assessment:
  - Both findings are valid evidence-correctness issues. Unknown check-run evidence should not be promoted to success, and repo-scoped GitHub credentials require path-aware lookup.
- TDD evidence:
  - Added RED coverage for status success plus unavailable check-runs payload, expecting `check_state=unknown`; pre-fix result was `success`.
  - Added RED coverage that credential fill input includes `path=limhaneul12/omx-agent-adapter.git`; pre-fix input omitted the path.
- Fix:
  - Check classification now preserves `unknown` when either combined status is unavailable or check-runs evidence is unknown before emitting success.
  - Credential lookup now builds the `git credential fill` input from the origin remote and includes `path=<owner>/<repo>.git` when the GitHub repo can be parsed.
- Verification so far:
  - Targeted RED tests confirmed both failures before implementation.
  - Targeted GREEN tests pass.
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`14 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`724 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=pending`, and `recommended_next_action=observe`.
- Next:
  - Commit/push, reply to Codex, and request another review.


## 18:40 KST — Address PR #10 follow-up review: preserve no-checks state

- Actor: Hermes.
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Codex P2: `_classify_check_state` made the `no_checks` branch unreachable after the unknown-check-runs hardening, so repositories with no legacy statuses and no check-runs could be classified as pending/unknown instead of explicitly `no_checks`.
- Objective assessment:
  - Valid. GitHub combined-status evidence can carry `state=pending` with an empty `statuses` array when no statuses are configured; combined with an explicit empty check-runs list, cockpit should preserve `check_state=no_checks` rather than waiting on nonexistent checks.
- TDD evidence:
  - Added RED coverage for combined status `{state: pending, statuses: []}` plus `{check_runs: []}`, expecting `check_state=no_checks`; pre-fix result was `pending`.
- Fix:
  - Added combined-status classification that distinguishes `no_statuses` from real `pending` and `unknown`.
  - `_classify_check_state` now returns `no_checks` when both legacy statuses and check-runs are explicitly absent, while preserving `unknown` for unavailable status/check evidence.
- Verification so far:
  - RED test confirmed failure before implementation.
  - Targeted GREEN tests pass.
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`15 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`725 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=success`, and `recommended_next_action=observe`.
- Next:
  - Optionally run local Codex CLI review as an additional owner-side review, commit/push, reply to Codex, and request another review.


## 18:51 KST — Local Codex CLI review follow-up: head-scoped clean markers and stale check-runs

- Actor: Hermes + local Codex CLI (`codex exec` read-only review).
- Branch: `feat/cockpit-pr-status-source`.
- Review input:
  - Local Codex HIGH: Codex no-major-issues issue comments were authored by Codex but not tied to the current PR head SHA, so stale clean comments from older commits could classify the current head as clean.
  - Local Codex MEDIUM: `_classify_check_runs` returned `success` after seeing any success-like completed run and ignored other completed unknown conclusions such as `stale`.
- Objective assessment:
  - Both findings are valid conservative-evidence issues. A clean marker should be scoped to the reviewed head, and unsupported check-run conclusions should not be silently absorbed into success.
- TDD evidence:
  - Added RED coverage for a Codex-authored clean marker referencing an old SHA, expecting `review_state=pending_or_unreviewed`; pre-fix result was `codex_no_major_issues`.
  - Added RED coverage for completed check-runs containing both `success` and `stale`, expecting `check_state=unknown`; pre-fix result was `success`.
- Fix:
  - Codex clean-marker detection now requires both Codex bot authorship and body text that mentions the current head SHA (full or 10-char short SHA). Existing positive fixtures now include the current head marker.
  - Unknown completed check-run conclusions now classify check-runs as `unknown` rather than allowing any success-like sibling to make the aggregate successful.
- Verification so far:
  - RED tests confirmed both failures before implementation.
  - Targeted GREEN tests pass.
  - `uv run pytest tests/runtime/test_github_pr_status.py -q` pass (`17 passed`).
  - `uv run ruff check src tests` pass.
  - `uv run pyrefly check src` pass.
  - `git diff --check` pass.
  - `uv run pytest -q` pass (`727 passed`).
  - `uv run pytest tests/test_cli_entrypoint.py -q` pass (`37 passed`).
  - Non-editable installed CLI rebuilt with `uv sync --no-editable --reinstall-package agent-remote`.
  - `.venv/bin/agent-remote version` pass (`agent-remote 0.1.0`).
  - `.venv/bin/agent-remote cockpit --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --help` pass.
  - `.venv/bin/agent-remote cockpit snapshot --cwd "$PWD"` emitted parseable JSON with `pull_request_status.status=open`, `review_state=pending_or_unreviewed`, `check_state=success`, and `recommended_next_action=observe`.
- Next:
  - Commit/push, reply to the GitHub Codex P2, and request another review.
