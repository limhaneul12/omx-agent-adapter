# 2026-05-08 Development Timeline

## 20:05 KST — Teamwork TypedDict transport hardening slice started

- Actor: Hermes
- Branch: `feat/teamwork-typed-transport-hardening`
- Status: completed
- 지금까지 완료된 것:
  - `main`을 최신 상태로 확인하고 typed transport hardening 전용 브랜치를 열었다.
  - `teamwork_types.py`, `team_api_transport.py`, `team_api_normalizers.py`, 관련 테스트를 읽고 첫 slice 후보를 `tasks/events/messages` collection과 envelope/data boundary narrowing으로 좁혔다.
  - `TeamApiTransportPayload`의 collection field를 `list[object]`로 좁히고 top-level `data`를 `dict[str, object]`로 고정했다.
  - `TeamApiListTasksNormalizedPayload`, `TeamApiReadEventsNormalizedPayload`, `TeamApiMailboxListNormalizedPayload`를 concrete typed list로 좁혔다.
  - loader가 non-list collection payload와 `None` filler field를 stable transport subset에서 제거하도록 바꿨다.
  - snapshot reader는 required collection이 빠진 경우 schema validation failure를 유지하면서, list인 경우에만 typed normalized payload를 만든다.
  - RED test로 non-list collection field dropping 기대 동작을 추가했다.
- 검증한 것:
  - `git switch main && git pull --ff-only && git switch -c feat/teamwork-typed-transport-hardening` 완료.
  - `uv run pytest tests/teamwork/test_team_api_snapshot.py::test_load_team_api_payload_drops_non_list_collection_fields -q` → fail 확인 후 pass 전환.
  - `uv run pytest tests/teamwork/test_team_api_snapshot.py tests/teamwork/test_team_api_cursor_consistency.py tests/types/test_teamwork_type_narrowing.py tests/types/test_transport_types.py -q` → `54 passed`
  - `uv run pytest tests/teamwork/test_team_api_snapshot.py tests/teamwork/test_team_api_mailbox_list.py tests/teamwork/test_team_api_cursor_consistency.py -q` → `56 passed`
  - `uv run pyrefly check src` → `0 errors`
  - `uv run ruff check src tests` → pass
- 다음에 해야 할 것:
  - follow-up slice에서 `status/config/manifest/snapshot` boundary를 더 세분화할지 검토한다.
  - 필요하면 `bridge_types.py`가 아니라 teamwork use-site 추가 테스트부터 더 촘촘히 고정한다.
- blocker / non-goal:
  - broad Pydantic migration은 하지 않는다.
  - 이 slice는 `teamwork_types.py` 중심 first hardening만 다루며 config/manifest schema redesign까지는 들어가지 않는다.

## 22:34 KST — Teamwork team-api msgspec data spec hardening

- Actor: Hermes
- Branch: `feat/teamwork-typed-transport-hardening`
- Status: completed
- 완료한 것:
  - 기존 shared `TeamApiDataSpec` + 긴 `isinstance(...)` chain을 operation-specific `msgspec.Struct` data spec으로 분리했다.
  - `list-tasks`, `read-events`, `mailbox-list`, `read-monitor-snapshot`, `read-config`, `read-worker-status` 전용 loader를 추가했다.
  - `tasks/events/messages`와 `worker/status` 같은 required operation fields는 transport boundary에서 malformed payload로 보고 `TeamworkSurfaceError`를 내도록 고정했다.
  - async team-api snapshot 함수는 command invocation 후 operation-specific loader를 호출하도록 얇게 정리했다.
  - 기존 Pydantic/public schema boundary는 유지했고, broad Pydantic migration은 하지 않았다.
- 검증한 것:
  - RED: 새 operation-specific msgspec spec/helper test 5개가 누락 symbol 및 downstream `ValidationError` 대신 expected `TeamworkSurfaceError`로 실패하는 것을 확인했다.
  - `uv run pytest tests/teamwork/test_team_api_snapshot.py -q` → `50 passed`
  - `uv run pytest tests/teamwork/test_team_api_snapshot.py tests/teamwork/test_team_api_mailbox_list.py tests/teamwork/test_team_api_cursor_consistency.py tests/types/test_teamwork_type_narrowing.py tests/types/test_transport_types.py -q` → `66 passed`
  - `uv run ruff check src tests && uv run pyrefly check src` → pass / `0 errors`
  - `uv run pytest -q` → `656 passed`
  - live read-only probes: `omx team api list-tasks`, `read-events`, `mailbox-list`, `read-monitor-snapshot` on `missing-team` plus adapter helper calls all succeeded.
- 다음에 해야 할 것:
  - PR review에서는 this branch가 now msgspec operation-specific transport hardening까지 포함한다고 보면 된다.
  - 다음 separate slice는 `bridge_types.py`보다 앞서 필요하면 `read-config` / `read-manifest` error-envelope evidence를 더 좁히는 쪽이 작다.
- blocker / non-goal:
  - Python 3.14-only API, stdlib `typing.TypedDict(extra_items=...)`, broad Pydantic upgrade는 사용하지 않았다.

## 23:17 KST — Live payload TypedDict/msgspec parallel hardening integrated

- Actor: Hermes + Codex subagents
- Branch: `feat/teamwork-typed-transport-hardening`
- Status: completed
- 완료한 것:
  - `docs/jobs/live-payload-typed-transport-hardening/`에 live payload evidence 기반 TypedDict/msgspec tightening 계획서를 작성했다. (`docs/jobs/`는 gitignored planning area라 커밋하지 않는다.)
  - Codex subagent 3개를 독립 worktree/branch로 병렬 실행했다.
    - `feat/teamwork-live-payload-type-tightening` → cherry-pick commit `302f76f`
    - `feat/execution-live-payload-type-tightening` → cherry-pick commit `700f5ce`
    - `feat/adapt-runtime-live-payload-type-tightening` → cherry-pick/amended commit `b0dde14`
  - `typing_extensions.TypedDict` 기반으로 `closed=True` / `extra_items=object`를 적용했다.
  - Team API envelope/data/error contract는 live `omx team api` payload shape에 맞춰 `ok: bool`, operation-specific required fields, closed adapter-owned transport payload로 좁혔다.
  - Execution payload는 usage/thread/item stable subset을 좁히되, unknown execution event/item variants는 `extra_items=object` passthrough로 유지했다.
  - Bridge/adapt/runtime payload는 live `omx adapt hermes ...` / `omx state ...` shape에 맞춰 required string/list/object fields를 transport boundary에서 검증하도록 helper를 분리했다.
- 검증한 것:
  - Codex stream targeted gates:
    - Teamwork stream: `64 passed`, ruff pass, pyrefly `0 errors`
    - Execution stream: `132 passed`, ruff pass, pyrefly `0 errors`
    - Adapt/runtime stream: `259 passed`, ruff pass, pyrefly `0 errors`
  - Integrated branch full gate:
    - `git diff --check` → pass
    - `uv run ruff check src tests` → pass
    - `uv run pyrefly check src` → `0 errors`
    - `uv run pytest -q` → `676 passed`
  - Live smoke/evidence:
    - `omx team api list-tasks/read-events/mailbox-list/read-monitor-snapshot/read-worker-status` → exit 0
    - `omx adapt hermes probe/status/envelope` → exit 0
    - `omx state list-active/get-status/read` → exit 0
    - `omx exec --skip-git-repo-check --json --output-last-message ...` → exit 0
- 다음에 해야 할 것:
  - push 후 PR review에서는 이 branch가 TypedDict PEP 728 options + operation-specific msgspec + live evidence tightening까지 포함한다고 보면 된다.
  - 남은 `object`는 upstream heterogeneous/raw passthrough 이유가 있는 곳만 남긴다.
- blocker / non-goal:
  - broad Pydantic migration은 하지 않았다.
  - stdlib `typing.TypedDict(extra_items=...)`는 쓰지 않았다.
  - Codex sandbox가 일부 live probe를 막아 owner process에서 live evidence를 수집해 subagent에게 주입했다.
