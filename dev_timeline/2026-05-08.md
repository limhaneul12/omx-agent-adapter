# 2026-05-08 Development Timeline

## 16:50 KST — Cockpit Ralph→Team evidence slice started

- Actor: Hermes
- Branch: `feat/agent-remote-cockpit`
- Status: completed
- 지금까지 완료된 것:
  - repo cockpit snapshot baseline 이후 Ralph→Team evidence slice를 시작했다.
  - explicit Team name을 받으면 실제 Team surface를 읽는 read-only 방향으로 범위를 고정했다.
- 검증한 것:
  - live preflight로 `omx` binary 존재를 확인했다.
  - missing team에 대해 status/tasks/events/worker-status surface가 구조화된 JSON을 반환하는지 확인했다.
- 다음에 해야 할 것:
  - explicit Team evidence를 cockpit snapshot에 연결하고 lane state derivation을 실제 Team status 기준으로 좁힌다.
- blocker / non-goal:
  - no Team launch
  - no cleanup
  - no Goal/Ralph mutation
  - no automatic worker fanout

## 17:04 KST — Cockpit Ralph→Team evidence slice implemented

- Actor: Hermes
- Branch: `feat/agent-remote-cockpit`
- Status: completed
- 지금까지 완료된 것:
  - `agent-remote cockpit snapshot --cwd . --team-name <team>` alias를 추가하고 실제 Team evidence를 embedded snapshot으로 올렸다.
  - Team status/tasks/events/worker-status surface를 읽어 Ralph→Team lane `team_observations`로 승격했다.
  - partial/missing Team read failure를 warnings로 낮춰 snapshot 전체 abort를 피했다.
- 검증한 것:
  - `uv run ruff check src tests` → pass
  - `uv run pyrefly check src` → `0 errors`
  - `uv run pytest -q` → `651 passed`
  - Homebrew Python `3.14.4` 타깃 cockpit test → `5 passed`
  - installed non-editable CLI smoke: `agent-remote cockpit snapshot --cwd . --team-name missing-team`
- 다음에 해야 할 것:
  - current feature boundary를 문서화하고 merge-ready stopping point를 명확히 남긴다.
- blocker / non-goal:
  - lane mutation 자동화는 아직 scope 밖이다.

## 17:50 KST — Current feature development scope recorded

- Actor: Hermes
- Branch: `feat/agent-remote-cockpit`
- Status: completed
- 지금까지 완료된 것:
  - `docs/current-feature-development-scope.md`를 추가했다.
  - read-only cockpit baseline, Python 3.13/3.14 support, explicit Team evidence read, 다음 typed transport hardening 방향을 기록했다.
- 검증한 것:
  - scope 문서가 현재 merge-ready stopping point와 non-goal을 포함하는지 확인했다.
- 다음에 해야 할 것:
  - broad Pydantic upgrade 대신 Teamwork transport hardening slice를 작은 단위로 시작한다.
- blocker / non-goal:
  - stdlib `typing.TypedDict` 기반 PEP 728 adoption은 현 시점 목표가 아니다.

## 18:51 KST — Handoff/timeline routine rule added

- Actor: Hermes
- Branch: `docs/handoff-timeline-routine`
- Status: completed
- 지금까지 완료된 것:
  - repository-root `dev_timeline/<YYYY-MM-DD>.md` 규칙을 새 기본 위치로 도입했다.
  - `docs/rules/handoff-and-timeline-rules.md`를 작성했다.
  - `AGENTS.md`, `docs/README.md`, `docs/current-feature-development-scope.md`, legacy `docs/dev_timeline.md`를 새 규칙에 맞게 연결했다.
- 검증한 것:
  - `git diff --check` → pass
  - `uv run ruff check src tests` → pass
  - `uv run pyrefly check src` → `0 errors`
  - `uv run pytest -q` → blocked by environment (`tmux` executable missing), docs change 자체와 직접 관련 없는 기존 환경 이슈 확인
  - current repo HEAD before this docs branch: `5fa45b7`
- 다음에 해야 할 것:
  - branch를 push하고 PR을 열어 reviewable handoff rule slice로 정리한다.
  - 필요하면 CI 환경 또는 local machine에 `tmux`를 준비해 full pytest green도 다시 확인한다.
- blocker / non-goal:
  - 이 slice는 repo rule/documentation hardening이며 product runtime behavior 변경은 하지 않는다.
  - local full pytest는 현재 machine의 missing `tmux` 때문에 완전 green을 재현하지 못했다.
